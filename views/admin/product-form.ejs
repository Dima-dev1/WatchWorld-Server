<!-- условие ? выражение1 : выражение2 -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
        form{
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 24px;
        }
    </style>
  </head>
  <body>
    <h1><%= product ? "Edit" : "Create" %> Product</h1>

    <% if (typeof error != "undefined" && error) { %>

    <div><%= error %></div>

    <% } %>
    <form action="<%= product ? `/admin/products/${product.id}/edit`: '/admin/products/create' %>" method="POST" enctype="multipart/form-data">
        <input type="text" placeholder="title" name="title" required value="<%= product ? product.title : ''%>">
        <textarea type="text" name="description" placeholder="description"></textarea>
        <input type="number" name="price" placeholder="price" required>
        <textarea type="characteristics" name="characteristics" placeholder="Oyster, 36 mm, platinum and diamonds"></textarea>
        <input type="file" accept="image/*">
        <% if (product && product.image){%> 
            <img src="<%=product.image %>" alt="">
            <%}%>
        <button type="submit">submit</button>
    </form>
  </body>
</html>
